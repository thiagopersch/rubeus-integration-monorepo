import Heading from "@/components/Heading";
import Base from "../Base";

const Home = () => {
  return (
    <Base>
      <Heading>Hello World Home</Heading>
    </Base>
  );
};

export default Home;
